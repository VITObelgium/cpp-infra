<?xml version="1.0" ?>
<plugins>
  <!-- adding the root path for the location of the plugins -->
  <pluginpath>/home/bino/local/lib/opaq</pluginpath>

  <!-- give list of plugin components... -->
  <plugin>
    <name>meteoPlugin</name>
    <lib>ECMWFMeteoProvider.so</lib>
  </plugin>

  <plugin>
    <name>obsPlugin</name>
    <lib>AsciiObsProvider.so</lib>
  </plugin>

  <plugin>
    <name>netPlugin</name>
    <lib>XMLAQNetProvider.so</lib>
  </plugin>

  <plugin>
    <name>h5dbPlugin</name>
    <lib>Hdf5Buffer.so</lib>
  </plugin>

  <plugin>
    <name>simpleAsciiForecastOutputPlugin</name>
    <lib>SimpleAscForecastOutputWriter.so</lib>
  </plugin>

  <plugin>
    <name>OVL_model1</name>
    <lib>OVL_IRCEL_model1.so</lib>
  </plugin>

  <plugin>
    <name>OVL_model2</name>
    <lib>OVL_IRCEL_model2.so</lib>
  </plugin>
  
</plugins>
