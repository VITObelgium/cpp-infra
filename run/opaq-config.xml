<?xml version="1.0" ?>
<opaq>
  <!-- if empty the console will get a log of output... -->
  <logfile>opaq.log</logfile>

  <!-- Master OPAQ configuration settings -->
  <pollutants ref="etc/pollutants.xml" />
  <plugins    ref="etc/plugins.xml" />
  <components ref="etc/components.xml" />

  <!-- Run configuration -->
  <runconfig  ref="etc/runconfig.xml" />
</opaq>
